# Models

The trained YOLO weights are not included in this repository because model files such as `best.pt` can be large.

Expected local path after training:

```text
littering_model/yolo11m_littering/weights/best.pt
```

You can train the model from the notebook, or place your trained weights in the expected path and set `SKIP_TRAIN = True`.
